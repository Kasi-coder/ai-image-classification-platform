# Models

After training, this folder contains the selected PyTorch model:

`best_model.pt`

The model metadata is saved in:

`model_metadata.json`

The model is produced by `scripts/train.py` and is not a placeholder.
